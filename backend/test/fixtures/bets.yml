player_bet_on_upcoming:
  user: player
  match: upcoming
  bet_type: "1"
  points_earned: 0.0

player_bet_on_locked:
  user: player
  match: locked
  bet_type: "X"
  points_earned: 0.0

admin_bet_on_upcoming:
  user: admin
  match: upcoming
  bet_type: "2"
  points_earned: 0.0

admin_bet_on_locked:
  user: admin
  match: locked
  bet_type: "1X"
  points_earned: 0.0
